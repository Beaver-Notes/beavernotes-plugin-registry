{
  "name": "beaver-notes-plugin-registry",
  "version": "1.0.0",
  "private": true,
  "description": "Community plugin registry for Beaver Notes",
  "scripts": {
    "validate": "node -e \"const plugins = require('./plugins.json'); console.log(plugins.length + ' plugins validated')\"",
    "generate": "node scripts/generate-plugins-json.js",
    "store:dev": "cd store && npm run dev",
    "store:build": "cd store && npm run build"
  }
}
